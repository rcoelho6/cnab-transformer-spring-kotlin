
# HTTP-BASE64 — SECURITY (moved)

As recomendações específicas de segurança para o fluxo `HTTP-BASE64` foram consolidadas em `../../segurança/CNAB-EXCHANGE.md`.

Por favor consulte o arquivo consolidado para diretrizes sobre TLS, OAuth2/mTLS, rate-limiting, validação e webhooks.

