# CONTAS E TRIBUTOS — ENDPOINTS SUMMARY

- POST /taxes/payments — solicita pagamento de tributo (codigoTributo, requestId, payer)
- GET /taxes/{id} — status
- Webhook: /webhooks/taxes

Headers: Authorization: Bearer <token>, X-Request-Id

