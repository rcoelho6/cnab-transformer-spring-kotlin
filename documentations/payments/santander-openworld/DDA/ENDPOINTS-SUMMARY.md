# DDA — ENDPOINTS SUMMARY

- POST /dda/presentations — apresenta título (requestId, nossoNumero, valor, sacado)
- GET /dda/{id} — status
- Webhook: /webhooks/dda

Headers: Authorization, X-Request-Id

